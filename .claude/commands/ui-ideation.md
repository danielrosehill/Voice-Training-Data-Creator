Review the current application

Ideate any UI/UX fixes that you think would make this utility easier to use for its purpose which (although limited) will be a repetitive workflow - so it's important that the UI is friendly and clear.

Implement.